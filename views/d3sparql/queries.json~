[
      {
	"name": "example_query",
	"query": "SELECT DISTINCT ?concept WHERE { ? s a ? concept.} LIMIT 5 "
	}, {
	"name": "example_query1",
	"query": `SELECT DISTINCT ?concept
		WHERE {
		    ?s a ?concept .
		} LIMIT 5`
	}
    ];

